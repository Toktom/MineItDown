package mineitdown

// Enums
BoardColors :: enum {
    Line,
    Background,
    Pattern,
}
CellType :: enum {
	Empty,
	Stone,
	MossyStone,
	MossyStoneCracked,
}
CellStatus :: enum {
	Active,
	Inactive,
}